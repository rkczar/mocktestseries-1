import { StubPage } from "@/components/admin/stub-page";

export const metadata = { title: "Announcements — Mock Test Series.in Admin" };

export default function Page() {
  return <StubPage title="Announcements" phase="Phase 6" />;
}
