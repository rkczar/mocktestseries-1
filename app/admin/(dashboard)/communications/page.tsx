import { StubPage } from "@/components/admin/stub-page";

export const metadata = { title: "Communications — Mock Test Series.in Admin" };

export default function Page() {
  return <StubPage title="Communications" phase="a later phase" />;
}
