import { StubPage } from "@/components/admin/stub-page";

export const metadata = { title: "AI Settings — Mock Test Series.in Admin" };

export default function Page() {
  return <StubPage title="AI Settings" phase="Phase 10" />;
}
