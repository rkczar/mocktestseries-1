import { StubPage } from "@/components/admin/stub-page";

export const metadata = { title: "Analytics — Mock Test Series.in Admin" };

export default function Page() {
  return <StubPage title="Analytics" phase="a later phase" />;
}
