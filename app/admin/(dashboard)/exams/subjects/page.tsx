import { StubPage } from "@/components/admin/stub-page";

export const metadata = { title: "Subjects — Mock Test Series.in Admin" };

export default function Page() {
  return <StubPage title="Subjects" phase="Phase 7" />;
}
