import { StubPage } from "@/components/admin/stub-page";

export const metadata = { title: "Bulk Import — Mock Test Series.in Admin" };

export default function Page() {
  return <StubPage title="Bulk Import" phase="Phase 7" />;
}
