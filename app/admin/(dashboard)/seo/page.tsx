import { StubPage } from "@/components/admin/stub-page";

export const metadata = { title: "SEO — Mock Test Series.in Admin" };

export default function Page() {
  return <StubPage title="SEO" phase="a later phase" />;
}
