import { StubPage } from "@/components/admin/stub-page";

export const metadata = { title: "Transactions — Mock Test Series.in Admin" };

export default function Page() {
  return <StubPage title="Transactions" phase="Phase 11" />;
}
