import { StubPage } from "@/components/admin/stub-page";

export const metadata = { title: "Revenue — Mock Test Series.in Admin" };

export default function Page() {
  return <StubPage title="Revenue" phase="Phase 11" />;
}
