import { StubPage } from "@/components/admin/stub-page";

export const metadata = { title: "Backup — Mock Test Series.in Admin" };

export default function Page() {
  return <StubPage title="Backup" phase="a later phase" />;
}
