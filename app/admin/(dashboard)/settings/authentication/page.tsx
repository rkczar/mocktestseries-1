import { StubPage } from "@/components/admin/stub-page";

export const metadata = { title: "Authentication — Mock Test Series.in Admin" };

export default function Page() {
  return <StubPage title="Authentication" phase="Phase 11" />;
}
