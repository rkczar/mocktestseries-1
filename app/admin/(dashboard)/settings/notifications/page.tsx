import { StubPage } from "@/components/admin/stub-page";

export const metadata = { title: "Notifications — Mock Test Series.in Admin" };

export default function Page() {
  return <StubPage title="Notifications" phase="Phase 11" />;
}
