import { StubPage } from "@/components/admin/stub-page";

export const metadata = { title: "General Settings — Mock Test Series.in Admin" };

export default function Page() {
  return <StubPage title="General Settings" phase="Phase 11" />;
}
